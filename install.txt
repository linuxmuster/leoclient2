Installation i J10.10

Prepare USB-Stick with mkusb
d
OK
i Install
l Live
Select iso
select Stick
Go

Install
Type F12 on boot --> boote menu
Select USB-HDD --> booting stick

Install Ubuntu:
--------------------------------------------------
* Deutsch
* Ubuntu installieren
* herunterladen Aktualisierungen
* Festplatte löschen, ubuntu installieren
* Berlin
* linuxadmin
* Muster!
* Neu starten



Virtualbox:
--------------------
wget -q https://www.virtualbox.org/download/oracle_vbox_2016.asc -O- | sudo apt-key add -
wget -q https://www.virtualbox.org/download/oracle_vbox.asc -O- | sudo apt-key add -


sudo apt-get update
sudo apt-get install dkms
sudo apt-get install virtualbox-5.1

Download Extension pack and install it.


sudo adduser user1
sudo adduser user2
sudo adduser user1 vboxusers
sudo adduser user2 vboxusers
sudo adduser linuxadmin vboxusers
(Abmelden und Anmelden)

Linuxmuster-client:
--------------------
# wget http://pkg.linuxmuster.net/linuxmuster.net.key -O - | sudo apt-key add -

In /etc/apt/sources.list:
deb http://pkg.linuxmuster.net/ xenial/ 

# sudo apt-get install leoclient2-leovirtstarter-client leoclient2-vm-printer
????? # sudo apt-get install linuxmuster-client-sudoers
--> nicht notwendig bei installation auf standard ubuntu

Install virtual Machine
-------------------------
sudo leoclient2-init

Ubuntu 17:04 server:
  * server17
  * ja
  * 8000
  * ja

  -> Virtualbox

  Allgemein:
     * Typ
     * Version
  Massenspeicher:
     * iso image wählen
     --> starten
     --> installieren
     --> runterfahren
     --> Virtualbox: beenden
     --> leoclint-init zippt noch, ABWARTEN  

Windows 10:
  * win10
  * ja
  * 23000 (minimum recommended)
  * ja

  -> Virtualbox

  Allgemein:
     * Typ
     * Version Windows 10 64 bit
System:
     * 4500 MB RAM
     * 4 CPU's
     * 64 MB Grafik RAM
     ????? 3D Beschleunigung ??? damit Guest additions das können?
Massenspeicher:
     * iso image wählen
     --> starten
     --> installieren
     --> runterfahren
     --> Virtualbox: beenden
     --> leoclint-init zippt noch, ABWARTEN  




Virtuelle Maschine starten
----------------------------
leovirtstarter2

Info:
cat /etc/leoclient2/machines/server17.conf
--> wo gibts virtuelle machinen (Pfad)


Drucken spooler/splitter
----------------------------
Entweder:
1) linuxmuster-client-extras
   (zieht andere Pakete nach zum Linuxmuster-client)

2) ohne linuxmuster-client-extras

sudo cp ./printer/leoclient2-splitter.desktop  /etc/xdg/autostart

sudo cp ./printer/leoclient2-spooler.desktop  /etc/xdg/autostart
